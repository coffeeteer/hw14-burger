var  = require(''); //connect to connection.js

//Create the methods that will execute teh necessary MySQL commands in the controllers. These are the methods you will need to use in order to retrieve and store data in your database.

selectAll();
insertOne();
updateOne();
//export the ORM in module.exports